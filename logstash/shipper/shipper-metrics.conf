input {
  file {
    type => "metrics"
    start_position => "beginning"
    path => [ "/sample/*.log" ]
  }
}
filter {
  if [type] == "metrics" {
    grok {
      match => { "message" => "%{GREEDYDATA}" }
    }
  }
}
